import { ShopDirective } from './shop.directive';

describe('ShopDirective', () => {
  it('should create an instance', () => {
    const directive = new ShopDirective();
    expect(directive).toBeTruthy();
  });
});
